// Mutual-consent unpair banner — reserved for when an unpair_requests table is added.
// Currently returns null since the simplified dissolve-pair flow is used instead.
export function UnpairPendingBanner() {
  return null;
}
